<%= form_for [@team_member, @notes] do |f| %>
  <% for @notes.each do |note|  %>
    <div class="container">
      <div class="row clearfix">
        <div class="col-md-12 column">
          <div class="row clearfix">
            <div class="col-md-6 column">
              Created:
            </div>
            <div class="col-md-6 column">
            </div>
          </div>
          <div class="jumbotron">
          </div>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
