<%= form_for(@team_position) do |f| %>
  <% if @team_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_position.errors.count, "error") %> prohibited this team_position from being saved:</h2>

      <ul>
      <% @team_position.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
